gcc5:
  docker:
    builder:
      image: epmkit/gcc5
      shell: /bin/bash
      home: /home/conan
    runner:
      image: ubuntu:xenial
      shell: /bin/bash
      home: /home/conan

  profiles:
    gcc5:
      description: |
        GNU GCC 5 build profile for x86_64 Linux with libstdc++.
    gcc5.d:
      description: |
        GNU GCC 5 build profile for x86_64 Linux with libstdc++ (DEBUG).

# =========================================================
# Microsoft Visual Studio 2019 profile.
# Regarding run-time details check for MD, MDd, MT, MTd on
# https://docs.microsoft.com/en-us/cpp/build/reference/md-mt-ld-use-run-time-library?view=vs-2019 
#
# =========================================================
vs2019:
  profiles:
    vs2019:
      description: |
        Microsoft Visual Studio 2019profile for x86_64.
        use the multithread-specific and DLL-specific version of the run-time library.
    vs2019.d:
      description: |
        Microsoft Visual Studio 2019profile for x86_64 (DEBUG).
        use the debug multithread-specific and DLL-specific version of the run-time library
    vs2019mt:
      description: |
        Microsoft Visual Studio 2019profile for x86_64.
        use the multithread, static version of the run-time library.
    vs2019mt.d:
      description: |
        Microsoft Visual Studio 2019profile for x86_64 (DEBUG).
        use the debug multithread, static version of the run-time library.
