get_filename_component(app_source_dir {{ '${%s_SOURCE_DIR} ' % name }} PATH)
##############################################################
#
# {{ name }} executable program
#
##############################################################
set(_SRCs)
set(_HDRs)

foreach(i ${CXX_HEADER_FILE_EXTENSION})
   file(GLOB_RECURSE _HDRs ${_HDRs} ${app_source_dir}/source/${i})
endforeach()
foreach(i ${CXX_SOURCE_FILE_EXTENSION} ${CXX_HEADER_FILE_EXTENSION})
   file(GLOB_RECURSE _SRCs ${_SRCs} ${app_source_dir}/source/${i})
endforeach()

add_executable({{ name }} ${_SRCs} ${_HDRs})
target_link_libraries({{ name }}  ${CONAN_LIBS} )

source_group(TREE ${app_source_dir} FILES ${_SRCs} ${_HDRs})


# Install configuration
set(_cmakedir_desc "Directory relative to CMAKE_INSTALL to install the cmake configuration files")
mark_as_advanced(CMAKE_INSTALL_CMAKEDIR)

# Install configuration
install(TARGETS {{ name }} EXPORT {{ name }}-targets
  RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR} COMPONENT {{ name }})