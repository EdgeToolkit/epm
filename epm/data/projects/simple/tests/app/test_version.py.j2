import unittest
from epm.api import API


class Test{{ name }}(unittest.TestCase):

    def test_{{ name }}(self):
        sandbox = API().sandbox()
        proc = sandbox.invoker('{{ name }}')
        proc.run()

        self.assertEqual(0, proc.exit_code)
        self.assertEqual('{{ name }} {{ version }}', proc.stdout().strip())
