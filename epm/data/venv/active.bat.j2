{% set name=config.venv.name %}
{% set environment=config.environment %}
@ECHO OFF
if defined EPM_VIRTUAL_ENVIRONMENT (
  echo "Already in virtual environment %EPM_VIRTUAL_ENVIRONMENT%, you have to exits this venv, then try again."
  exit/b 1
)

set EPM_VIRTUAL_ENVIRONMENT={{name}}
set CONAN_USER_DIR=

epm venv banner

title [epm venv] - {{name}}
prompt $p [epm venv]@{{name}} $_$$