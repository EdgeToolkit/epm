
#- apt: name=docker.io state=present  
#- apt: name=python3 state=present
#- apt: name=python3-pip state=present
#- command: python3 -m pip install {{ pip_options }} docker-py 
- name: Get known docker image (debug)
  command: echo In dev enviroment
  register: result
  when: epm_ci_env == "dev"

- name: Get known docker image
  command: echo In production
  register: result
  when: not epm_ci_env == "dev"

- name: show exec infomation
  debug:
    var: result

