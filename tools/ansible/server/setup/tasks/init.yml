
- apt: name=docker.io state=present
- apt: name=python3 state=present
- apt: name=python3-pip state=present
- command: python3 -m pip install docker-py -i {{ proxy.pypi }} --trusted-host {{ proxy.pypi_trust }} --proxy={{ proxy.http }
