# Base conan image for GNU GCC5
# We have do this image instead of use conanio/gcc5-x86, only beacuse
# the official images not work for Windows Docker Desktop (faild at cmake)
# 
# Version: {{version}}
# 
#
FROM conanio/gcc5-x86:1.18.0

LABEL maintainer="Mingyi Zhang <mingyi.z@outlook.com>"

RUN sudo pip install  {{pip_options}} --disable-pip-version-check --upgrade pip \
    && sudo pip uninstall -y conan conan-package-tools \
    && sudo pip install conan=={{conan_version}}  {{pip_options}}


